/** Цвета, которые используются в приложении */
export type Colors = {
  bg: string;
  surface: string;
  text: string;
  textMuted: string;
  border: string;

  primary: string;
  primaryBg: string;

  success: string;
  successBg: string;

  danger: string;
  dangerBg: string;
};

/** Тема, используемая в приложении */
export type ThemeMode = 'light' | 'dark';
